# Статус потока C

## Готово

- Добавлены `run.py` по интерфейсам §3 Plan.md с проверкой уникальности и полноты gid на объединениях.
- Добавлен экран Streamlit/PyVis: поиск, top-30 и фильтры, карточка, кластер, направленное окружение, скачивание CSV.
- Обновлены зависимости, Docker, Compose, README и диаграмма Mermaid + SVG.

## Проверки и блокеры

- На момент начала работы `pipeline/` и модули A/B отсутствовали в рабочем дереве. Полный расчёт и проверка CSV возможны после их появления.
- Docker Engine в среде ранее не отвечал; команду Compose надо перепроверить при доступном движке.
- `python -m py_compile run.py viz/app.py`, `git diff --check` и `docker-compose config --quiet` прошли.
- Python-окружение сейчас не содержит `pandas`, `pyarrow`, `streamlit` и `pyvis`; экран и расчёт на настоящих данных ещё не запускались.

## Следующий шаг

После появления модулей A/B сверить реальные сигнатуры с §3 Plan.md, выполнить два полных запуска, проверить §8 на CSV и открыть экран локально. Несовпадения контрактов передать соответствующему владельцу.
